# Database
DATABASE_URL="postgresql://postgres:password@localhost:55432/zahrat_al_hayat"

# NextAuth
# Generate a strong secret (min 32 chars): openssl rand -base64 32
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"

# Stripe (use live keys in production, test keys locally)
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_PUBLISHABLE_KEY="pk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."

# Resend (email)
RESEND_API_KEY="re_..."
RESEND_FROM_EMAIL="noreply@zahrat-al-hayat.com"

# App
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_..."

# Seed passwords (min 8 chars). Required when seeding in production.
# If unset in development, the seed generates strong random passwords and prints them once.
SEED_ADMIN_PASSWORD=""
SEED_TEACHER_PASSWORD=""
SEED_STUDENT_PASSWORD=""

# Set to "1" to skip startup env validation (e.g. for tooling/CI). Do not use in production.
# SKIP_ENV_VALIDATION="0"
